spring:
  application:
    name: chat-book-cloud-gateway

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

server:
  port: 8080

--- ## 日志
logging:
  level:
    org.springframework.cloud.gateway: info
